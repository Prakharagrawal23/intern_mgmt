ALTER TABLE users 
ADD COLUMN batch_code BIGINT;

ALTER TABLE users
ADD CONSTRAINT FK_batchcode 
FOREIGN KEY (batch_code) REFERENCES internship_batches(batch_code);

UPDATE users SET batch_code = NULL;

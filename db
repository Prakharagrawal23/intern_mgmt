not available
﻿
react refresh:37 Download the React DevTools for a better development experience: https://react.dev/link/react-devtools
UserTasks.jsx:25 
 Each child in a list should have a unique "key" prop.

Check the render method of `UserTasks`. See https://react.dev/link/warning-keys for more information.

Tasks
ID	Description	Status	Deadline
do this	PENDING	17 Dec 2025, 05:30 am

import React, { useEffect, useState } from "react";
import { getUserTasks } from "../../api/userApi";
import Navbar from "../../components/Navbar";
import Sidebar from "../../components/Sidebar";
import formatDate from "../../utils/formatDate";

export default function UserTasks() {
  const userId = localStorage.getItem("userId");
  const [tasks, setTasks] = useState([]);

  useEffect(() => {
    if (!userId) return;
    getUserTasks(userId).then(res => setTasks(res.data)).catch(console.error);
  }, [userId]);

  return (
    <div className="layout">
      <Navbar />
      <Sidebar items={[{ to: "/user/dashboard", label: "Dashboard" }]} />
      <div className="main">
        <h3>Tasks</h3>
        {tasks.length === 0 ? "No tasks found." :
          <table className="table">
            <thead><tr><th>ID</th><th>Description</th><th>Status</th><th>Deadline</th></tr></thead>
            <tbody>
              {tasks.map(t => (
                <tr key={t.taskId}>
                  <td>{t.taskId}</td>
                  <td>{t.description}</td>
                  <td>{t.status}</td>
                  <td>{formatDate(t.deadline)}</td>
                </tr>

              ))}
            </tbody>
          </table>
        }
      </div>
    </div>
  );
}
